# YearArk 毕业纪念册系统 — 演示视频脚本

> 总时长：约 7 分 30 秒 | 录制建议：提前登录好系统和管理端，准备 10+ 张测试照片

---

## 一、开场：为什么做这个项目（0:00 – 0:50）

**画面：** 打开首页，展示公开纪念册画廊，缓慢滚动

**台词：**

> 介绍我的毕业设计项目 YearArk，一个 AI 驱动的在线纪念册生成平台。
>
> YearArk 的核心理念就是 —— 用户只管提供回忆，AI 负责把回忆变成作品。自动生成一本有章节、有文案、排版精美的纪念册

---

## 二、产品全貌：核心功能一览（0:50 – 1:30）

**画面：** 快速切换展示各个页面（Dashboard → 创建 → 详情 → 预览 → 分享）

**台词：**

> 先快速过一下产品的核心功能。
>
> 一。注册登录功能，密码前端哈希后传输，认证用的 Sa-Token。
>
> 登录后进入个人面板，这里展示我所有的纪念册。点击创建，填写名称和描述，然后选一个模板。（模板是后台管理员配置的，每个模板包含封面页、章节页、内容页等不同类型的页面，**每页都有自己的 HTML 模板和 Schema 定义，规定了图片槽位数、文字槽位数和字数限制**。这套设计让我们可以灵活扩展不同风格，不需要改代码。）
>
> 
>
> 第二，素材收集。这里有两种方式：自己上传，或者通过邀请链接让朋友匿名上传，不需要注册。
>
> 我在邀请管理里创建一个邀请链接，设置一个访问码。然后把这个链接发给同学，他们打开链接、输入访问码，就能直接上传照片，全程不需要注册账号。
>
> 
>
> 第三，一键生成。点一个按钮，用户看到的只是一个按钮和一个进度状态，但背后是一条四步 AI 流水线：AI 自动分析照片进行分组、匹配模板、生成文案，输出完整纪念册。

> 第四，预览、与在线编辑。对 AI 的结果不满意可以手动微调文案和图片。：预览页可以逐页翻阅整本纪念册，每张照片都根据焦点坐标做了智能裁剪，构图很舒服。
>
> 如果觉得某段文案不够好，进入编辑器可以逐页修改。编辑器会根据 Schema 展示所有可编辑的槽位，改完实时预览，保存时自动校验。
>
> 
>
> 第五，分享。生成的纪念册可以公开到首页画廊，也可以通过链接分享给朋友在线翻阅。
>

管理后台：

**第一块，模板管理。** 。管理员可以在这里创建和维护模板套件，每个模板点进详情，能看到它包含的所有模板页 （—— 封面页、章节页、内容页、纯文字页。每个模板页可以设置页面类型、关联 Schema、编写 H5 模板内容，还支持上传预览图和在线预览渲染效果。）

**第二块，Schema 管理。** Schema 定义了每种页面布局的槽位结构 —— 有几个图片位、几个文字位、每个文字位的字数上限。这是模板和 AI 之间的桥梁：AI 生成文案时会读取 Schema 的约束，确保生成的内容能正确填入模板。管理员新增一个 Schema，就等于定义了一种新的页面布局能力。



以上就是 YearArk 的完整演示 







---

## 三、注册登录 → 创建纪念册（1:30 – 2:10）

**画面：** 注册 → 登录 → Dashboard → 创建纪念册 → 选模板

**台词：**

> 注册登录就不多说了，标准的用户名 + 邮箱注册，密码前端哈希后传输，认证用的 Sa-Token。
>
> 登录后进入个人面板，这里展示我所有的纪念册。点击创建，填写名称和描述，然后选一个模板。
>
> 模板是后台管理员配置的，每个模板包含封面页、章节页、内容页等不同类型的页面，**每页都有自己的 HTML 模板和 Schema 定义，规定了图片槽位数、文字槽位数和字数限制**。这套设计让我们可以灵活扩展不同风格，不需要改代码。

---

## 四、素材上传 + 重点：匿名协作上传（2:10 – 3:20）⭐

**画面：** 上传照片 → 创建邀请链接 → 无痕模式打开分享页 → 匿名上传

**台词：**

> 纪念册创建好了，接下来上传素材。我先自己传几张照片。
>
> 但做纪念册往往不是一个人的事 —— 毕业旅行的照片分散在每个人手机里。所以我们设计了一个匿名协作上传功能。
>
> 我在邀请管理里创建一个邀请链接，设置一个访问码。然后把这个链接发给同学，他们打开链接、输入访问码，就能直接上传照片，全程不需要注册账号。
>
> 
>
> 技术上简单说一下实现：匿名用户验证访问码后，后端会生成一个独立的匿名 Token，和正式用户的认证体系完全隔离。每个匿名用户有独立的 tokenId，所有上传都关联到这个 ID，创建者可以看到谁上传了什么，也支持匿名用户自己删除自己的素材。安全性和可追溯性都有保障。

---

## 五、重点：一键生成 — AI 如何把照片变成纪念册（3:20 – 4:40）⭐

**画面：** 点击生成按钮 → 等待/展示状态 → 切到流程图或代码简要讲解

**台词：**

> 素材收集完毕，点击「生成纪念册」。这是整个产品最核心的能力。
>
> 用户看到的只是一个按钮和一个进度状态，但背后是一条四步 AI 流水线：
>
> **第一步，视觉理解与智能分组。** 系统把所有照片一次性传给视觉大模型，让它理解每张照片的场景、活动、氛围，然后按主题把照片分成 2 到 5 个章节。比如海边的照片归为一组叫「海风与浪花」，聚餐的照片归为「围桌的笑声」。同时还会为每张照片标注视觉焦点位置，后面渲染时用来智能裁剪，确保主体不会被切掉。
>
> **第二步，模板匹配。** 这个等下单独讲。
>
> **第三步，文案生成。** 这里用了一个两轮策略：先并发生成所有内容页的文案，然后把这些文案作为上下文，再并发生成章节页和封面页的文案。为什么要两轮？因为封面和章节标题需要概括整章甚至整本的内容，如果不知道里面写了什么就没法概括。这个设计确保了封面文案和实际内容不会脱节。
>
> **第四步，组装与校验。** 把所有页面数据打包返回，后端逐页做 Schema 校验后写入数据库。
>
> 整个过程通过 RabbitMQ 消息队列异步处理，Java 后端发消息、Python AI 服务消费处理、结果再通过 MQ 回传，前端轮询状态，生成完成自动跳转预览。

---

## 六、重点：模板匹配算法 — 让每一页都不一样（4:40 – 5:30）⭐

**画面：** 展示生成结果中不同布局的页面对比，然后切到代码简要讲解

**台词：**

> 模板匹配要解决的问题是：怎么**把分好组的照片分配到不同模板页上**，同时让整本纪念册看起来不单调？
>
> 如果简单地按顺序循环分配，比如 1 张图模板、2 张图模板、3 张图模板这样轮转，翻几页就能看出规律，体验很差。
>
> 我们的做法是**「分桶 + 加权随机」**：
>
> 先把所有内容模板按能放几张图分桶 —— 1 图桶、2 图桶、3 图桶。然后每次分配时，从能放下剩余图片的桶里加权随机选一个。权重就是桶里模板的种类数，模板越丰富的桶被选中概率越高。
>
> 同时有个防重复机制：如果上一页选了某个桶，这一页选同一个桶的权重减半。桶内部也有轮转，确保同一个模板不会连续出现。
>
> 最终效果就是：每一页的布局都不一样，有的放 1 张大图，有的放 2 张并排，有的放 3 张拼贴，看起来很自然，像是人工排版的。

---

## 七、预览、编辑与分享（5:30 – 6:10）

**画面：** 展示预览页翻页效果 → 进编辑器改文案 → 展示分享链接 / 公开画廊

**台词：**

> 生成完成，来看看效果。预览页可以逐页翻阅整本纪念册，每张照片都根据焦点坐标做了智能裁剪，构图很舒服。
>
> 如果觉得某段文案不够好，进入编辑器可以逐页修改。编辑器会根据 Schema 展示所有可编辑的槽位，改完实时预览，保存时自动校验。
>
> 做好的纪念册可以设为公开，展示在首页画廊供大家浏览；也可以通过链接分享给朋友在线查看。整个从创建到分享的闭环就完成了。

---

## 八、管理端介绍（6:10 – 7:10）

**画面：** 切换到管理端后台（ruoyi-admin），依次展示各管理页面

**台词：**

> 刚才演示的都是用户端，接下来看一下管理端。我们基于 RuoYi-Vue-Plus 框架单独搭建了一套管理后台，用的是 Vue 3 + Ant Design Vue + Vben Admin。
>
> 管理端主要分三块：
>
> **第一块，模板管理。** 这是整个系统的内容基础。管理员可以在这里创建和维护模板套件，每个模板点进详情，能看到它包含的所有模板页 —— 封面页、章节页、内容页、纯文字页。每个模板页可以设置页面类型、关联 Schema、编写 H5 模板内容，还支持上传预览图和在线预览渲染效果。
>
> **第二块，Schema 管理。** Schema 定义了每种页面布局的槽位结构 —— 有几个图片位、几个文字位、每个文字位的字数上限。这是模板和 AI 之间的桥梁：AI 生成文案时会读取 Schema 的约束，确保生成的内容能正确填入模板。管理员新增一个 Schema，就等于定义了一种新的页面布局能力。
>
> **第三块，系统管理。** 这部分是 RuoYi 框架自带的能力，包括用户管理、角色权限、菜单配置、字典管理、操作日志、在线用户监控、缓存管理等等。我们在此基础上扩展了 YearArk 的业务菜单和权限控制，比如模板管理的增删改查都有独立的权限码。
>
> 管理端和用户端是完全独立部署的两个前端应用，共享同一套后端 API，通过角色权限区分访问范围。

---

## 九、总结（7:10 – 7:30）

**画面：** 回到首页或展示架构图

**台词：**

> 总结一下，YearArk 解决的核心问题是让纪念册制作从「手动排版」变成「上传即生成」。产品上通过匿名协作降低了素材收集门槛，技术上通过视觉大模型 + 模板匹配算法 + 两轮文案生成实现了高质量的自动排版。管理端则提供了完整的模板配置和系统运维能力，让整个平台可持续运营。
>
> 技术栈方面，后端 Spring Boot + MyBatis Plus，用户端和管理端分别是两套 Vue 3 + TypeScript 应用，AI 服务 Python FastAPI，通过 RabbitMQ 解耦。
>
> 以上就是 YearArk 的完整演示，谢谢大家。

---

## 附：时间分配总览

| 段落 | 内容 | 侧重 | 时长 |
|------|------|------|------|
| 一 | 开场：为什么做这个项目 | 产品 | 50s |
| 二 | 产品全貌 | 产品 | 40s |
| 三 | 注册登录 & 创建纪念册 | 产品+技术 | 40s |
| 四 | ⭐ 素材上传 & 匿名协作 | 产品为主 | 70s |
| 五 | ⭐ 一键生成流水线 | 产品+技术 | 80s |
| 六 | ⭐ 模板匹配算法 | 技术为主 | 50s |
| 七 | 预览、编辑与分享 | 产品 | 40s |
| 八 | 管理端介绍 | 产品+技术 | 60s |
| 九 | 总结 | 综合 | 20s |
| **合计** | | | **约 7 分 30 秒** |

## 附：录制小贴士

- 提前准备 10-15 张不同场景的照片（聚餐、户外、合影等），生成效果更好
- 匿名上传环节用浏览器无痕模式模拟匿名用户，效果更直观
- 生成需要等 AI 处理，建议提前生成好一本，录制时直接展示结果避免冷场
- 管理端提前配好至少一套模板（含 Schema + 模板页），演示时直接展示成品，再演示新增操作
- 讲技术时点到为止，重点说「为什么这么设计」而不是「代码怎么写的」
- 语速不要太快，重点段落可以适当放慢，给观众消化的时间
